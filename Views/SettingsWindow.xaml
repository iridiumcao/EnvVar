<Window x:Class="EnvVar.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EnvVar.Views"
        mc:Ignorable="d"
        Title="{DynamicResource Title_Settings}"
        Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource Settings_HistoryLimit}" VerticalAlignment="Center" Margin="0,0,0,10" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox x:Name="HistoryCountTextBox" Width="100" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBlock Text="{DynamicResource Settings_HistoryLimit_Hint}" VerticalAlignment="Center" Opacity="0.6" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Btn_Save}" Width="80" Margin="0,0,10,0" Click="SaveButton_Click" IsDefault="True" />
            <Button Content="{DynamicResource Btn_Cancel}" Width="80" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
